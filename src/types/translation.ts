export type Vector = Array<number>
export type position = {
    x: number, 
    y: number,
    z: number
}
export type direction = 'up' | 'down' | 'right' | 'left'
export type size = {
    width: number,
    height: number
}